#include <vector>

namespace sort {
std::vector<int> insertion_sort(std::vector<int> &input);
void insertion_sort_inplace(std::vector<int> &input);
std::vector<int> merge_sort(std::vector<int> &input);
} // namespace sort
